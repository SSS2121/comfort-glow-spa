{
  "name": "bilingual-beauty-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "images:build": "node scripts/optimize-images.mjs",
    "build": "npm run images:build && vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check:browser": "node scripts/browser-check.mjs",
    "check:security": "node scripts/security-check.mjs",
    "check": "npm run test && npm run check:security && npm run build"
  },
  "devDependencies": {
    "@fontsource/cormorant-garamond": "^5.3.0",
    "@fontsource/manrope": "^5.3.0",
    "sharp": "^0.35.4",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
